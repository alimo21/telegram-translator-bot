export const metadata = {
  title: 'ربات ترجمه تلگرام',
  description: 'ترجمه خودکار عربی به فارسی',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
